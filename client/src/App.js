import './App.css';

import { useApi } from './hooks/use-api.js';

function App() {
  /*const { response } = useApi("GET", "/everything");
  console.log(response);*/

  return (
    <div className="App">
      <header className="App-header">
        <p>
        </p>
      </header>
    </div>
  );
}

export default App;
